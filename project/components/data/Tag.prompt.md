Neutral attribute tag — distinct from StatusPill (which carries semantic state). Square-ish corners, cream fill.

```jsx
<Tag>35 г</Tag> <Tag>Флоупак</Tag> <Tag>LV</Tag>
```

Use for non-stateful metadata: packaging type, size, language, channel.
